---
name: sage
description: >
  Workflow orchestrator. Routes user requests to the right Sage workflow
  (build, fix, architect, research, etc.). Use as entry point.
version: "1.0.0"
tier: workflow
---

# sage (workflow skill — fixture baseline)

Workflow skille (`tier: workflow`) NIE dostają `agents/openai.yaml` —
zachowują pełne reactive routing. Pokazane tu jako baseline różnicy
od direct skille.
