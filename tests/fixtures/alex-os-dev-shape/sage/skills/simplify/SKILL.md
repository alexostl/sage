---
name: simplify
description: >
  Simplifies code by removing duplication and improving readability.
  Use when the user says "uprość ten kod", "make this simpler",
  "refactor for readability", "clean up this function", or
  "remove duplication here".
version: "1.0.0"
tier: direct
---

# simplify (fixture stub)

Pre-cycle state: NO `agents/openai.yaml`. M3 will deploy one with
`policy.allow_implicit_invocation: false`.
