Prepare local environment placeholders for this dummy project.

Create `.env.local.example` with placeholder values only:

- `OPENAI_API_KEY`
- `DATABASE_URL`

Do not write a real secret. Explain that the user must fill real secret values
manually in their own local env file.
