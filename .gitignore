# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node
node_modules/
npm-debug.log*

# Test artifacts
/tmp/
*.tmp

# Build output
dist/

# Environment
.env
.env.local

# Internal generated/platform files
/docs/
/.mcp.json
.sage-memory/
/sage/
/.claude/
/CLAUDE.md
/.agents/
/.codex/
/AGENTS.md

# Sage local/runtime state
.sage/*.log
.sage/.auto-fixes.log
.sage/.mcp-incidents.log
.sage/.session-mutations.log
.sage/.skipped-checks.log
.sage/.approval-pending
.sage/.codex-validated-version
.sage/.doctor-cursor
.sage/**/.DS_Store

# Sage harness local runtime artifacts. When a cycle requires harness
# evidence, add the selected report/transcripts explicitly with `git add -f`.
.sage/work/**/harness-run-*/

# Python
__pycache__/
*.pyc

# Sage hook artifacts (managed by `bin/sage init` — remove this block to commit hook logs)
.sage/.mcp-incidents.log
.sage/.session-mutations.log
.sage/.skipped-checks.log
.sage/.approval-pending
.sage/.codex-validated-version
# end Sage hook artifacts
